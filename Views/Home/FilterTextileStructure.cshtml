@{
    ViewData["Title"] = "Filter Textile Structure";
}

<header class="bg-dark py-5">
    <div class="container px-5">
        <div class="row gx-5 align-items-center justify-content-center">
            <h1 class="display-5 fw-bolder text-white mb-2 text-center">Filter Category: Textile Structure</h1>
        </div>
    </div>
</header>

<div class="container text-center">
    <h3>Please select a value for Textile Structure below:</h3>
    <br />

    <form method="post" asp-controller="Home" asp-action="BurialList" id="filterCategory">
        <input type="hidden" name="filterType" value="Textile Structure" />

        <label>Textile Structure:</label>
        <select class="my-select" name="filterValue" id="textileStructure">
            @foreach (var x in ViewBag.MummyDataTextileStructure)
            {
                <option value="@x">@x</option>
            }
        </select>
        <br />
        <br />
        <br />
        <button class="btn btn-primary" type="submit">Filter</button>
    </form>

</div>
